[project]
name = "RAG"
version = "1.0.0"
description = "42 Retrieval Augmented Generation, by npapot"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.19.1",
    "pydantic>=2.12.5",
    "torch>=2.10.0",
    "transformers>=5.3.0",
]